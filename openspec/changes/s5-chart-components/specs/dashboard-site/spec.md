## ADDED Requirements

### Requirement: Generated multi-page static site with no JavaScript toolchain

The site SHALL be a set of static HTML pages under `site/`, generated by
`scripts/build_site.py` from `series/*.json` and `pages/*.json`, served by GitHub Pages
with no framework, no backend and no JavaScript toolchain (no npm, no bundler, no
transpiler): the JavaScript under `site/js/` SHALL be native ES modules shipped exactly
as written. Charts SHALL be rendered with Plotly.js.

#### Scenario: Page loads standalone

- **WHEN** any generated page is opened (locally or on GitHub Pages)
- **THEN** it renders using only the committed `site/css` and `site/js` files plus the
  Plotly.js library, with no server-side rendering or API calls beyond fetching the
  static JSON files

### Requirement: Section navigation

Every page SHALL carry a generated two-row nav: the site name linking to `/` and the
three sections (Economy, Markets, Rates & Yields); then, on section, chart and composite
pages, that section's charts and composite pages with the current item marked. Which
series exist and where they sit is defined by their descriptors' `presentation` blocks;
the nav is agnostic to the series set.

#### Scenario: Section page lists its charts

- **WHEN** the user opens `/economy/`
- **THEN** the top row marks Economy and the second row lists every series whose
  `presentation.sections` includes `economy`, in `order`

### Requirement: Cards fetch at mount and plot into a visible container

Each card SHALL fetch its series' JSON when the card is mounted, which happens as the
page loads; stats, table, badge, About and exports SHALL render as the fetch resolves;
the chart SHALL be drawn by the card chrome immediately after, into a container that is
visible and laid out. No chart SHALL be drawn into a hidden (`display:none`) container,
because Plotly sizes a hidden container to a 700px fallback and keeps that size.

#### Scenario: Chart fills the card on first draw

- **WHEN** any page's cards mount
- **THEN** each chart's figure width equals its container's width on the first draw

## MODIFIED Requirements

### Requirement: Dark theme and per-tab data loading

The site SHALL use a dark theme by default (S5b adds the light palette and the toggle)
and SHALL load each card's data by fetching the corresponding `/data/<id>.json` with a
root-relative path, so pages in subdirectories resolve the same file.

#### Scenario: Data fetched per series

- **WHEN** a card mounts on any page
- **THEN** the page fetches `/data/<id>.json` for that series and renders its chart,
  stats and table from that response

### Requirement: Fetch failure surfaces an error message

A card SHALL display a human-readable error message in place of its chart when its data
fetch fails (network error, missing file, malformed JSON), rather than rendering empty
or broken visuals. Other cards on the page SHALL continue to function.

#### Scenario: A JSON file is missing

- **WHEN** a card's fetch of `/data/<id>.json` fails
- **THEN** that card's chart area shows an error message identifying the failure, and
  the remaining cards render normally

### Requirement: Minimal mobile breakpoint

The site SHALL provide a `max-width: 640px` CSS breakpoint that reduces page padding,
collapses grids to one column, and sets every chart container's height by its variant
class (`full` 300px; `half` and `third` 300px). No chart id SHALL be named in CSS.

**Known gap:** wide data tables (notably the yield-curve table) are not yet adapted for
narrow viewports and may require horizontal scrolling. Full mobile responsiveness is
measured at S12, not part of this change.

#### Scenario: Page viewed at mobile width

- **WHEN** a grid page is rendered at a viewport width ≤ 640px
- **THEN** page padding is reduced, cards stack in one column, and every chart is at
  the mobile height regardless of its series

## REMOVED Requirements

### Requirement: Single-file static dashboard
**Reason**: The site is now a generated set of pages with committed `site/css` and
`site/js` sources; the single inline file is retired at the S7 cutover.
**Migration**: Replaced by "Generated multi-page static site with no JavaScript
toolchain" above and the `site-build` capability.

### Requirement: Tabbed navigation across series
**Reason**: Flat tabs are replaced by the three-section nav and one page per chart.
**Migration**: Replaced by "Section navigation" above; a card's Chart sub-tab is the
default on every mount, so "returning to a card shows the chart" holds by construction.

### Requirement: Default tab on page load
**Reason**: There are no tabs; every page shows all of its cards.
**Migration**: `/` is the curated grid from `pages/home.json`.

### Requirement: Data fetched on page load, not lazily
**Reason**: The page-level deferred-draw registry (`renderWhenVisible`/`onTabShown`)
existed only because tabs hid containers; cards are never hidden at mount.
**Migration**: Replaced by "Cards fetch at mount and plot into a visible container"
above and the `chart-components` mount lifecycle; fetching stays eager.
