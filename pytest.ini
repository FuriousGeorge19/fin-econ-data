[pytest]
testpaths = tests
markers =
    network: requires network access and FRED_API_KEY (skipped automatically if the key is unset)
    staleness: checks published data freshness against as_of.due_by (deselect to run offline / not gate a deploy)
